<%= form_with(model: student_progress_report) do |form| %>
  <% if student_progress_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student_progress_report.errors.count, "error") %> prohibited this student_progress_report from being saved:</h2>

      <ul>
        <% student_progress_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_name, style: "display: block" %>
    <%= form.text_field :student_name %>
  </div>

  <div>
    <%= form.label :feedback, style: "display: block" %>
    <%= form.textarea :feedback %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
